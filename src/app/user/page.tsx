import { FC } from 'react';

// import classes from '.';

const page: FC = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: 100 }}>
      This page is not available now, but I'm working on it
    </div>
  );
};

export default page;
